{
  "title": "Insecure Direct Object Reference Leaks User Data",
  "modelType": "STRIDE",
  "type": "Information disclosure",
  "tags": [
    "idor",
    "authorisation",
    "api"
  ],
  "score": "High",
  "description": "API endpoints accept a resource ID as a path parameter without verifying the requesting user owns that resource. An attacker increments the ID to access other users' data.",
  "mitigation": "Enforce object-level authorisation on every request. Use opaque, non-sequential identifiers (UUIDs). Implement automated IDOR scanning in the CI pipeline."
}